#include "main.h"
#include <string.h>

/**
 * _strcmp - compares strings
 *
 * @s1: s1 is a char
 * @s2: s2 is a char
 * Return: Returns s1
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
