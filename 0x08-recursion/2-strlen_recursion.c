#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: s is a char
 * Return: Return int on success
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
