#include <stdio.h>

/**
 * main - Program to print single digit numbers
 *
 * Return: Returns 0 at success
 */
int main(void)
{
	int a;

	for (a = 48; a <= 57; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
